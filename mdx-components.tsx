import { useMDXComponents as getNextraThemeComponents } from 'nextra-theme-docs'

// Use nextra-theme-docs' useMDXComponents which includes the `wrapper` component
// that renders the sidebar, TOC, and main content grid layout.
export { getNextraThemeComponents as useMDXComponents }
